import styled from "styled-components/native";

export const EditAddressSafe = styled.SafeAreaView`
  flex:1;
  background: ${({ theme }) => theme.colors.default};
`;

export const EditAddressContainer = styled.View`
  padding: 24px 30px;
`;